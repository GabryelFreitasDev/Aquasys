<?xml version="1.0" encoding="utf-8" ?>
<local:BasePages xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:local="clr-namespace:Aquasys.App.MVVM.Views"
             xmlns:s="clr-namespace:Aquasys.App.MVVM.ViewModels.Vessel"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:de="clr-namespace:DevExpress.Maui.Editors;assembly=DevExpress.Maui.Editors"
             xmlns:dxco="clr-namespace:DevExpress.Maui.Controls;assembly=DevExpress.Maui.Controls"
             xmlns:dx="clr-namespace:DevExpress.Maui.Core;assembly=DevExpress.Maui.Core"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewmodel="clr-namespace:Aquasys.App.MVVM.ViewModels.Vessel"
             xmlns:view="clr-namespace:Aquasys.App.MVVM.Views.Vessel"
             x:Class="Aquasys.App.MVVM.Views.Vessel.HoldPage">

    <Grid>
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="10">

                <Label 
                    Text="Hold Registration" 
                    FontAttributes="Bold" 
                    FontSize="20"/>

                <de:TextEdit
                    x:Name="BasementNumber"
                    LabelText="Name"
                    Text="{Binding HoldModel.BasementNumber, Mode=TwoWay}"/>

                <de:TextEdit
                    x:Name="Agent"
                    LabelText="Agent"
                    Text="{Binding HoldModel.Agent, Mode=TwoWay}"/>

                <de:TextEdit
                    x:Name="Cargo"
                    LabelText="Cargo"
                    Text="{Binding HoldModel.Cargo, Mode=TwoWay}"/>

                <de:NumericEdit
                    x:Name="Capacity"
                    Value="{Binding HoldModel.Capacity, Mode=TwoWay}"
                    LabelText="Capacity"/>

                <de:NumericEdit
                    x:Name="ProductWeight"
                    Value="{Binding HoldModel.ProductWeight, Mode=TwoWay}"
                    LabelText="Product Weight"/>

                <de:TextEdit
                    x:Name="LoadPlan"
                    Text="{Binding HoldModel.LoadPlan, Mode=TwoWay}"
                    LabelText="Load Plan"/>

                <dx:DXButton 
                    ButtonType="Accent"
                    Icon="setaparadireita.png"
                    IconAspect="AspectFill"
                    IconPlacement="Right"
                    BackgroundColor="{StaticResource Primary}"
                    Content="Inspection"
                    Command="{Binding InspectionCommand}"/>
                
                <dx:DXButton 
                    ButtonType="Accent"
                    Content="Save"
                    Command="{Binding SaveHoldCommand}"
                    IsVisible="{Binding IsNew}"/>

            </VerticalStackLayout>
        </ScrollView>
        
    </Grid>
</local:BasePages>